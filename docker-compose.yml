services:
  printforge:
    image: printforge:latest
    build: .
    container_name: printforge
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - printforge-data:/data

volumes:
  printforge-data:
