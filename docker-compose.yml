services:
  3d-printing-business:
    image: 3d-printing-business:latest
    build: .
    container_name: 3d-printing-business
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - 3d-printing-business-data:/data

volumes:
  3d-printing-business-data:
